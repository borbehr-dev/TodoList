{
  "name": "Dart on Ubuntu 22.04",
  // Используем официальный образ Ubuntu 22.04 (jammy) от Microsoft
  "image": "mcr.microsoft.com/devcontainers/base:jammy",

  // Автоматически устанавливаем Dart через встроенные фичи
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:1": {},
    "ghcr.io/devcontainers/features/common-utils:1": {
      "configureZshAsDefaultShell": true
    }
  },

  // Устанавливаем официальное расширение Dart для VS Code в облаке
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code"
      ]
    }
  },

  // Скрипт, который выполнится сразу после создания контейнера (ставим Dart SDK)
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y apt-transport-https wget gnupg && sudo sh -c 'wget -qO- https://dl-ssl.google.com/linux/linux_signing_key.pub | gpg --dearmor -o /usr/share/keyrings/dart.gpg' && sudo sh -c 'echo \"deb [signed-by=/usr/share/keyrings/dart.gpg] https://storage.googleapis.com/download.dartlang.org/linux/debian stable main\" > /etc/apt/sources.list.d/dart_stable.list' && sudo apt-get update && sudo apt-get install -y dart",

  // Настройки окружения (добавляем Dart в PATH, чтобы команда dart работала в терминале)
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/usr/lib/dart/bin"
  }
}
